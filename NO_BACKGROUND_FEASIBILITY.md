# 无背景模式 —— 可行性分析

> 分支:`feature/no-background-mode`(基于 `main` @ `8538772`)
> 状态:**已实现并端到端验证**(本文档保留为设计与实测记录)
> 结论:**可行**,走"绿幕生成 + ffmpeg 抠像"路线;透明通道路线不可行

---

## 1. 需求澄清

现有三个布局:

| 布局 | 画面构成 |
|---|---|
| `classic-pip` | 游戏画面 + 右下角一个方框,框里是**主播和他的房间** |
| `stacked` | 上下/左右分屏,一半游戏,一半**主播和他的房间** |
| `streamer-only` | 只有**主播和他的房间**,没有游戏画面 |

所以"现在的选项里都是带背景画面的"指的是:**主播自带背景**。头像生成时 `setting` 是必填字段(`components/AvatarGenerator.tsx:133`),提示词里还硬编码了一句 `Background must be out of focus/depth of field`(`services/prompts.ts:168`)。

**无背景模式** = 把主播自己的背景抠掉,人物直接叠在游戏画面上,像真实主播的绿幕摄像头 overlay。

> 注意:如果需求其实是"不要游戏画面",那 `streamer-only` 已经满足,不需要新功能。本文按"抠掉主播自身背景"分析。

---

## 2. 两条技术路线

### 路线 A:真透明通道(带 alpha 的视频)—— ❌ 不可行

需要图片带 alpha,且视频保留 alpha。实测:

```
要求 "transparent background, output PNG with alpha channel"
→ 返回 image/png，但 PNG colorType = 2 (RGB，无 alpha 通道)
```

图片模型**不产出 alpha**,即使明确要求。而 Omni / Veo 的输出是 h264 / yuv420p,视频容器层面也没有 alpha 可用。这条路不用再考虑。

### 路线 B:绿幕生成 + ffmpeg 抠像 —— ✅ 可行

```
头像按纯绿背景生成
   → Omni 从这张图生成视频（绿幕随之保留）
   → ffmpeg chromakey 去掉绿色
   → overlay 到 gameplay 上
```

全链路已实测跑通,见下。

---

## 3. 实测证据

### 3.1 绿幕能生成,且视频不会破坏它

| 环节 | 背景色 | 最大通道标准差 |
|---|---|---|
| 头像图(极简提示词) | `rgb(9,245,8)` | **1.7** |
| 该图生成的视频 t=0.2s | `rgb(18,239,21)` | 1.4 |
| 同一视频 t=1.5s | `rgb(19,237,23)` | 1.3 |
| 同一视频 t=3.0s | `rgb(19,237,22)` | 1.5 |

**关键发现:视频生成不会破坏绿幕。** h264 + yuv420p 压缩之后背景依然很均匀,这是整个方案能成立的前提。

### 3.2 抠像真的抠掉了背景

对上面的视频做 `chromakey` 并叠到测试图上,采样叠加区域:

| 采样点 | 结果 | 说明 |
|---|---|---|
| 叠加框左上角 | `rgb(0,0,254)` | 测试图的蓝色 → **绿幕被去掉,背景透出来了** |
| 叠加框右上角 | `rgb(255,255,255)` | 测试图的白色 → 同上 |
| 叠加框中心 | `rgb(158,149,143)` | 人物皮肤 → **主体保留** |
| 叠加框外 | `rgb(0,255,255)` | 纯测试图 |

### 3.3 边缘质量:无绿边,但过渡偏软

在原始 1280×720 分辨率下扫过人物边界:

```
边界内侧 6 像素的绿偏移: -17, -17, -16, -17, -17, -19
→ 全为负值，说明没有绿色溢出到人物上（无绿边）

alpha 过渡: 0 0 0 21 92 117 159 174 205 221 255 ...
→ 约 9px 半透明过渡带
```

**无绿边是好消息**,省掉了 despill 处理。9px 软边是 **yuv420p 色度下采样**的必然结果(色度分辨率只有亮度的一半),不是抠像参数问题。在 PiP 那种缩到 10% 面积的尺寸下基本看不出;若做全屏大尺寸叠加会比较明显。

### 3.4 稳定性:提示词一复杂,绿幕就变脏 —— 但仍可抠

用接近真实的提示词(带人物描述)生成 4 张:

| # | 人物 | 背景色 | 标准差 |
|---|---|---|---|
| 1 | 粉色耳机女主播 | `rgb(17,174,57)` | 24.3 |
| 2 | 戴眼镜大胡子男 | `rgb(55,174,77)` | 15.5 |
| 3 | 卡通狐狸 | `rgb(5,210,19)` | 14.7 |
| 4 | 长辫女主播 | `rgb(20,170,55)` | 14.9 |

标准差从极简提示词的 1.7 涨到 15–24,模型给背景加了光影。

**我一开始按 `sd<12` 判定"不可抠",4 张全不合格 —— 这个阈值是我拍的,而且拍错了。** 实测抠像结果:

| 容差 | 图1 (sd 24.3) | 图2 (sd 15.5) | 图3 (sd 14.7) | 图4 (sd 14.9) |
|---|---|---|---|---|
| `similarity=0.10` | ✅ 背景 100% / 人物 100% | ✅ 100% / 100% | ✅ 100% / 100% | ✅ 100% / 100% |
| `similarity=0.20` | ✅ 100% / 100% | ❌ 人物 0% | ✅ 100% / 100% | ⚠️ 人物 85% |
| `similarity=0.30` | ❌ 人物 0% | ❌ 人物 0% | ✅ 100% / 100% | ❌ 人物 0% |

**4/4 在 `similarity=0.10` 下都能干净抠出。** 真正的风险不是绿幕不够纯,而是**容差调大会吃掉人物**——所以要用保守容差配合实测出来的背景色,而不是写死一个绿色常量。

### 3.5 最坏情况端到端

把最差的那张(sd 24.3)完整跑一遍:

```
头像 sd=24.3 → Omni 生成视频 → 视频 sd: 24.5 / 24.1 / 23.9（三个时间点）
→ 实测背景色 0x17A940
→ similarity=0.10 → 背景去除 100%，人物保留 100%  ✅
→ similarity=0.15 → 背景去除 100%，人物保留 100%  ✅
→ similarity=0.20 → 人物保留 72.7%  ❌
```

**视频生成不会让绿幕恶化**(24.5 → 23.9 基本持平),最坏情况依然可抠。

---

## 4. 实现方案

### 4.1 关键设计决策

**抠像色要实测,不能写死。** 背景色在 `rgb(5,210,19)` 到 `rgb(55,174,77)` 之间飘,写死 `0x00FF00` 会抠不干净。做法:合成前从视频四角采样求均值,把实测色喂给 `chromakey`。这套测量代码在可行性验证里已经跑通,可直接移植。

**容差保守,并且要验证结果。** `similarity=0.10` 四例全过,但没有一个固定值对所有素材都安全(0.20 就有两例吃人物)。建议:先用 0.10,抠完采样校验"背景去除率 / 主体保留率",不达标再小步上调,而不是一开始就放宽。

### 4.2 新布局类型

现有 `classic-pip` 会画圆角矩形 + 6px 白边(`lib/compose.js`),那是"摄像头方框"的视觉语言,和抠像人物叠加是冲突的——抠出来的人不该再套个框。

需要新增布局,建议 `overlay-cutout`:

| 项 | 做法 |
|---|---|
| 主播尺寸 | 比 PiP 大(人物本身已无背景,不占视觉重量),建议画面高度的 45–60% |
| 位置 | 贴底边,左右可选,像真实直播的立绘 |
| 圆角/白边/阴影 | **全部不要** |
| 主播播完后 | 不能像 PiP 那样填黑(会出现一个黑块),应改为淡出或直接消失 |

`types.ts:3` 的 `LayoutType` 加一个值,`lib/compose.js` 的 `computeLayout` 加一个分支。

### 4.3 头像生成要改的地方

| 位置 | 改动 |
|---|---|
| `services/prompts.ts:168` | 硬编码的 `Background must be out of focus/depth of field` 在此模式下必须换成纯绿幕指令,并额外要求"不要把绿色用在人物、衣服、头发上" |
| `components/AvatarGenerator.tsx:133` | `setting` 现在是必填。此模式下应置灰并说明"该模式不需要背景描述" |
| Autopilot 表单 | 同上,`avatarPrompt` 里的背景部分要相应处理 |

### 4.4 校验门

参照现有的做法,在头像确认之前加一道自动校验:测量四角背景的绿主导性与均匀度,不合格就自动重生成。这道校验能挡住"模型没听懂,生成了带房间的图"这种情况——那种图抠出来会是灾难。

判据不用我原先那个错误的 `sd<12`,而应该是:**绿主导(G−R>40 且 G−B>40)+ 试抠一次并校验背景去除率**。

### 4.5 与 Autopilot 的关系

Autopilot 现在只有 `vary-script` 一种变体模式,新布局对它是透明的——只要 `computeLayout` 支持,批量链路不用改。但确认闸门上应该说明"此模式下主播背景会被抠掉",否则用户看到一张纯绿背景的候选图会以为出错了。

---

## 5. 风险与代价

| 风险 | 程度 | 应对 |
|---|---|---|
| 9px 软边,大尺寸叠加时可见 | 中 | 限制主播尺寸不超过画面高度 60%;或在抠像后做 1px 收边 |
| 模型偶尔无视绿幕指令 | 中 | 校验门 + 自动重生成(与现有 `content_blocked` 重试同一套机制) |
| 背景色漂移 | 已解决 | 实测采样,不写死 |
| 容差调大吃掉人物 | 中 | 保守容差 + 抠像后结果校验 |
| 人物穿绿色衣服会被抠穿 | **高** | 提示词明确禁止;校验门难以自动发现,建议在闸门文案里提示用户 |
| 深色/发丝边缘细节丢失 | 低 | chromakey 对发丝一般,但在 PiP 尺寸下不明显 |

**"人物穿绿衣服"是最难自动兜住的一项**:抠像会在人物身上开洞,而自动校验很难区分"衣服被抠穿"和"背景被正常抠掉"。这一项只能靠提示词约束加用户确认。

---

## 6. 工作量估计

| 阶段 | 内容 |
|---|---|
| N1 | `lib/compose.js` 加 `overlay-cutout` 布局 + 背景色采样 + chromakey,配像素级测试 |
| N2 | 绿幕头像提示词与 `setting` 字段的条件处理 |
| N3 | 头像校验门(绿主导 + 试抠校验)与自动重生成 |
| N4 | 前端布局选项、Autopilot 闸门文案 |
| N5 | 文档与端到端验证 |

N1 是主体,和之前 P1 的性质相同(纯 ffmpeg + 可像素级验证),风险最低。N3 是质量保障的关键。

---

## 7. 实施结果(2026-09-07)

三项决策已定:抠掉主播自身背景、保持角落小尺寸、给 `classic-pip` 加开关(不新增布局)。

| 阶段 | 状态 | 要点 |
|---|---|---|
| N1 | ✅ | `lib/compose.js` 加 `removeBackground`;不画圆角白边;尾帧透明 |
| N2 | ✅ | 头像提示词绿幕变体,禁止在人物身上用绿色 |
| N3 | ✅ | 绿幕校验门 + 自动重试,交互与 Autopilot 两条路径共用同一判据 |
| N4 | ✅ | 向导与 Autopilot 表单开关;浏览器与服务端两套合成都实现 |
| N5 | ✅ | 端到端验证 + 文档 |

### 实施中纠正的关键设计

本文档第 3.4 节记录的"绿幕不够纯"结论**部分是度量错误造成的**。实施时连续错了三次:

1. `sd<12` 阈值是拍的,拍错了
2. 采样四角——头肩取景下肩膀占满底边,底部两角不是背景
3. 改成只取顶部——有的生成里头部占满顶部,同样不成立

改用与位置无关的度量后真相清楚:7 张真实素材的绿像素抠除率 98.7~99.3%、非绿像素保留 99.95~100%。**抠像一直是好的,错的是尺子。**

同时否掉了 `chromakey`:它按"到某个颜色的距离"抠,而生成的绿幕带着人物打光(实测一张图从 `rgb(1,123,2)` 到 `rgb(80,221,76)`),距离判据必然漏掉渐变一端。最终采用比值判据 `g > 1.5×max(r,b)`,对明暗天然免疫,且不需要采样颜色。

一个失败的尝试也记录在案:怀疑提示词里 `livestream quality lighting effect` 导致背景被打光,改成平光后绿幕质量无实质改善(背景去除 88%/74% → 88%/83%)。根因不是措辞。

### 端到端实测

真实链路:绿幕头像(71% 绿,一次通过)→ Omni 生成视频(绿占比 70.3%)→ 完整合成。

成品 1920×1080 / 6.00s / 含音轨,PiP 框 `x=38 y=700 w=607 h=342`,六个采样点**零绿色残留**:框内透出 gameplay 蓝、中心是主播肤色、上边无白边。

同素材开关对照(同一坐标):关闭时 `rgb(13,177,67)`(绿幕可见),开启后 `rgb(14,46,254)`(透出 gameplay)。

---

## 8. 原始待确认(留档)

1. **需求确认**:是"抠掉主播自身背景"(本文分析的),还是"不要游戏画面"(`streamer-only` 已支持)?
2. **主播尺寸与位置**:贴底边的大立绘,还是仍旧角落小尺寸?这直接决定 9px 软边是否会成为问题。
3. **是否替换还是新增**:`overlay-cutout` 作为第四个布局并存,还是给 `classic-pip` 加一个"去背景"开关?

---

## 附录:可复现的验证方法

背景均匀度测量(可直接用于校验门):

```
四角各取 12×12 采样 → 求 R/G/B 均值与标准差
绿主导判据: G−R > 40 且 G−B > 40
抠像色: 直接用采样均值,不写死常量
```

抠像效果验证:

```
抠像后叠到品红底(255,0,255)上
→ 四角应变品红(背景去除率)
→ 中心应非品红(主体保留率)
两者都 >95% 才算通过
```
